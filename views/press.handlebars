<div class="container">
    <div class="row">
        {{#each card}}
            <div class="col-md-4">
                <img src="{{this.img}}" alt="...">

                <p class="title">{{this.title}}</p>
                <p class="date">{{this.date}}</p>
                <p class="message">{{this.message}}...</p>
                <a href="/press/{{url}}">Read more</a>
            </div>
        {{/each}}
    </div>
</div>
