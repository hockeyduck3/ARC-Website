<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>ARC Homepage</title>

        {{!-- Bootstrap css link --}}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        
        {{!-- Google Fonts links --}}
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans&display=swap" rel="stylesheet"> 

        {{!-- Font Awesome --}}
        <script src="https://kit.fontawesome.com/0a5e91de56.js" crossorigin="anonymous"></script>

        {{!-- css links --}}
        <link rel="stylesheet" href="/css/main.css">
    </head>

    <body id='page'>
        <div class="pageTopper">
            <p class="address">24 Delay St. Danbury CT. 06810 · execdir@arcofpeace.org · 203-792-9450</p>
            <p class="address shortAddress">24 Delay St. Danbury CT. 06810</p>

            <a class="donateTop" href="#">Donate</a>

            <div class="socialMediaTop">
                {{!-- Facebook --}}
                <a href="https://www.facebook.com">
                    <i class="fab fa-facebook fa-lg"></i>
                </a>

                {{!-- Twitter --}}
                <a href="https://twitter.com">
                    <i class="fab fa-twitter fa-lg"></i>
                </a>

                {{!-- Youtube --}}
                <a href="https://www.youtube.com">
                    <i class="fab fa-youtube fa-lg"></i>
                </a>
            </div>
        </div>

        {{!-- Navbar --}}
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">ARC</a>

            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        {{#if home}}
                            <a class="nav-link active" href="/">Home</a>
                        {{else}}
                            <a class="nav-link" href="/">Home</a>
                        {{/if}}
                    </li>

                    <li class="nav-item">
                        {{#if about}}
                            <a class="nav-link active" href="/about">About Us</a>
                        {{else}}
                            <a class="nav-link" href="/about">About Us</a>
                        {{/if}}
                    </li>

                    <li class="nav-item">
                        {{#if programs}}
                            <a class="nav-link active" href="/programs">Programs</a>
                        {{else}}
                            <a class="nav-link" href="/programs">Programs</a>
                        {{/if}}
                    </li>

                    <li class="nav-item">
                        {{#if gallery}}
                            <a class="nav-link active" href="/gallery">Gallery</a>
                        {{else}}
                            <a class="nav-link" href="/gallery">Gallery</a>
                        {{/if}}
                    </li>

                    <li class="nav-item">
                        {{#if involved}}
                            <a class="nav-link active" href="/involved">Get Involved</a>
                        {{else}}
                            <a class="nav-link" href="/involved">Get Involved</a>
                        {{/if}}
                    </li>

                    <li class="nav-item donateNav">
                        <a class="nav-link" href="#">Donate</a>
                    </li>
                </ul>
            </div>
        </nav>

        {{{ body }}}

        {{!-- Footer --}}
        <footer>
            <div class="container">
                <div class="footerTop">
                    <form>
                        <p class="signUpTitle">Sign Up for the Latest Updates on ARC</p>

                        <div class="form-group">
                            <input type="email" class="form-control" id="emailInput" placeholder="Enter Email Address">
                            <button type="submit" class="btn signUpBtn">Sign Up</button>
                        </div>
                    </form>

                    <div class="socialMediaFooter">
                        {{!-- Facebook --}}
                        <a href="https://www.facebook.com">
                            <i class="fab fabFooter fa-facebook fa-2x"></i>
                        </a>

                        {{!-- Twitter --}}
                        <a href="https://twitter.com">
                            <i class="fab fabFooter fa-twitter fa-2x"></i>
                        </a>

                        {{!-- Youtube --}}
                        <a href="https://www.youtube.com">
                            <i class="fab fabFooter fa-youtube fa-2x"></i>
                        </a>
                    </div>
                </div>
            </div>
        </footer>

        {{!-- jQuery/Bootstrap links --}}
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ho+j7jyWK8fNQe+A12Hb8AhRq26LrZ/JpcUGGOn+Y7RsweNrtN/tE3MoK7ZeZDyx" crossorigin="anonymous"></script>

        {{!-- JS Links --}}
        <script src="/js/main.js"></script>
    </body>
</html>